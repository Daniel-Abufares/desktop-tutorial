# פרויקט: ניהול פיננסי אוטומטי

מטרת הפרויקט: לאחד תנועות מ-3 מקורות (בנק אוצר החייל, בנק One Zero, שני כרטיסי כאל),
לבנות יומן תנועות מרכזי, לסווג לקטגוריות, ולהפיק דו"ח + דשבורד חודשי.
**כלל-על: לעולם לא מתחברים לאתר/אפליקציה של בנק/אשראי ולא מזינים סיסמה בשום שלב. עובדים אך ורק על קבצים שהמשתמש כבר ייצא בעצמו.**

## מתי להריץ את התהליך
כשהמשתמש אומר משהו כמו "עבד את החודש", "תעדכן את הדשבורד", "יש קבצים חדשים ב-inbox" וכו'.

## הגדרה חד-פעמית (בפעם הראשונה בלבד, לפני התהליך החודשי הרגיל)
בצע את כל הפעולות הבאות לבד, ברצף, ושאל רק את מה שבאמת מסומן כ"שאל":

1. **Git**: אם `.git` לא קיים בשורש הפרויקט, הרץ `git init -q`, `git add -A`, `git commit -q -m "init"`. זה נותן היסטוריה מלאה של היומן הפיננסי בלי מאמץ נוסף מהמשתמש.
2. **זיהוי שני כרטיסי כאל**: לכל קובץ ב-`inbox/cal/`, נסה לזהות מתוך תוכן הקובץ (4 ספרות אחרונות של הכרטיס, אם מופיעות) לאיזה כרטיס הוא שייך. אם אי אפשר לדעת מהקובץ עצמו - **שאל** את המשתמש פעם אחת "קובץ X שייך לאיזה כרטיס?" ואז תמיד תריץ `ingest.py cal <path> --account cal-XXXX` עם 4 הספרות כשם החשבון, כדי שהדשבורד יציג את שני הכרטיסים בנפרד.
3. **הטבות צבירה לכרטיסים** (לצורך אופטימיזציית נקודות/מיילים): בדוק אם `config/card_benefits.yaml` כבר מולא. אם לא - **שאל** את המשתמש שאלה אחת מרוכזת: "לכל אחד מהחשבונות/כרטיסים (otzar-hahayal, onezero, cal-XXXX, cal-YYYY) - יש הטבת צבירה/קאשבק מיוחדת בקטגוריה מסוימת (למשל 'x3 נקודות על מסעדות')? אם כן, פרט לפי קטגוריה." מלא את התשובה ב-`config/card_benefits.yaml` לפי הפורמט שבקובץ. אם המשתמש אומר שאין לו מידע כזה כרגע - השאר את הקובץ ריק ודלג על שלב האופטימיזציה בהרצות הבאות (בלי לשאול שוב, עד שהמשתמש יעדכן ביוזמתו).
4. **בדיקת Gmail (רק אם יש לך גישה לכלי Gmail בסביבה הזו)**: אם מחובר MCP של Gmail, חפש מיילים מהדומיינים הרלוונטיים (כאל, הבנק הבינלאומי/אוצר החייל, One Zero) עם קובץ מצורף (לא רק קישור התחברות) שמכיל דוח תנועות/חיובים. אם נמצא כזה בהתמדה - הצע למשתמש (אל תפעיל לבד) לעבור לשליפה אוטומטית מהמייל במקום ייצוא ידני, ותסביר בדיוק מה זה אומר לפני שמדליק את זה. אם אין לך גישה לכלי Gmail בסביבה - דלג על השלב הזה בלי להזכיר אותו.

לאחר ההגדרה החד-פעמית, המשך ישירות לתהליך הרגיל למטה על הקבצים שכבר נמצאים ב-inbox.

## התהליך המלא (תעשה הכל לבד, שאל כמה שפחות)

1. **סרוק** את `inbox/otzar-hahayal/`, `inbox/onezero/`, `inbox/cal/` (לא כולל תת-תיקיית `processed/`) לקבצי csv/xlsx חדשים.
   אם אין קבצים חדשים באף תיקייה — דווח בקצרה ועצור.

2. **לכל קובץ, לכל מקור:**
   - בדוק אם יש כבר מיפוי שמור עבור המקור הזה ב-`config/parsers.yaml`.
   - **אם כן** — הרץ `python3 scripts/ingest.py <source> <path>` (המיפוי השמור ישמש אוטומטית).
   - **אם לא (מקור/פורמט חדש)** — פתח את הקובץ בעצמך (head של השורות הראשונות / קריאה עם pandas), זהה איזו עמודה היא תאריך, איזו תיאור/בית עסק, ואיזו סכום (או חובה/זכות בנפרד), זהה מאיזו שורה מתחיל הטבלה בפועל (אקספורטים ישראליים נוטים לכלול כמה שורות כותרת/מטא-דאטה למעלה) והאם הקידוד הוא UTF-8 או Windows-1255.
     שמור את המיפוי הזה ב-`config/parsers.yaml` תחת מפתח המקור, ואז הרץ את `ingest.py` עם המיפוי החדש.
     אם משהו באמת לא ברור (למשל אי אפשר להבחין אילו 2 עמודות הן תאריך מול תאריך ערך) — זו נקודה לגיטימית לשאול את המשתמש שאלה אחת קצרה וממוקדת. חוץ ממקרה כזה, אל תשאל.

     **קריטי - כיוון הסימן**: כשיש עמודת `amount` יחידה (לא `amount_debit`+`amount_credit` נפרדים), חובה לקבוע נכון האם מספר חיובי שם מייצג הוצאה או הכנסה, **לפני** שסומכים על התוצאה.
     - בדוחות כרטיס אשראי ("סכום חיוב" וכו') - כמעט תמיד מספר חיובי = הוצאה. יש לסמן `expense_positive: true`.
     - בתנועות עו"ש (בנק/One Zero) - בד"כ הוצאה כבר מגיעה שלילית (`expense_positive: false`, ברירת המחדל). **תמיד תוודא** ע"י בדיקת שורת הכנסה ידועה (למשל משכורת) בקובץ - אם היא מופיעה כחיובית, זה תקין; אם ההוצאות מופיעות כחיוביות וההכנסה שלילית, יש להפוך (`expense_positive: true`).
     - טעות בכיוון הזה הופכת את כל הדשבורד ללא נכון (הוצאות ייראו כהכנסות) - אל תנחש, בדוק בפועל מול לפחות שורה אחת שאתה יודע בוודאות אם היא הכנסה או הוצאה.

3. **נרמול**: לכל תנועה בקובץ החדש בנה רשומה בסכימה האחידה:
   `date, source, account, description, amount, currency, category, raw_hash`
   - `amount`: שלילי = הוצאה, חיובי = הכנסה/זיכוי.
   - `source`: אחד מ- `otzar-hahayal` / `onezero` / `cal-card1` / `cal-card2` (אם יש שני קבצי כאל, זהה איזה כרטיס לפי 4 הספרות האחרונות שמופיעות בקובץ עצמו, לא לפי שם הקובץ).
   - `raw_hash`: hash (למשל md5) על `date+description+amount+source` — משמש לדדופ.

4. **דדופליקציה**: לפני הוספה ל-`data/transactions.csv`, השווה `raw_hash` מול מה שכבר קיים בקובץ. אל תוסיף כפילויות (למשל אם המשתמש מייצא טווח תאריכים חופף בטעות).

5. **קטגוריזציה**: לכל תנועה חדשה, נסה להתאים קטגוריה לפי מילות מפתח ב-`config/categories.yaml` (חיפוש substring בתיאור, לא תלוי אותיות גדולות/קטנות).
   - אם נמצאה התאמה — קבע קטגוריה.
   - אם לא נמצאה התאמה **וסכום התנועה מעל 300 ₪** — שאל את המשתמש שאלה אחת: "X ₪ ב-'<תיאור>' — לאיזו קטגוריה?" עם 2-3 קטגוריות מוצעות מתוך הרשימה הקיימת, ואז **הוסף את מילת המפתח ל-`categories.yaml`** כדי שבפעם הבאה זה יסווג לבד.
   - אם לא נמצאה התאמה וסכום קטן — סווג כ"אחר" בלי לשאול.

6. **עדכן** את `data/transactions.csv` עם כל הרשומות החדשות (מסודר לפי תאריך).

7. **הפק דו"ח חודשי**: הרץ `python3 scripts/build_dashboard.py`. הסקריפט קורא את `data/transactions.csv` ומייצר:
   - `reports/<YYYY-MM>/dashboard.html` — דשבורד עצמאי (נפתח בדפדפן, לא צריך שרת).
   - `reports/<YYYY-MM>/summary.md` — סיכום טקסטואלי קצר, כולל **חריגות** (קטגוריות שסטו משמעותית מהממוצע ההיסטורי שלהן - מחושב אוטומטית בתוך הסקריפט, אין צורך בשלב נפרד).

8. **הפק דו"ח שנתי/רבעוני**: הרץ `python3 scripts/annual_report.py`. מייצר `reports/annual/<year>/annual-report.html` עם מגמה שנתית, פילוח קטגוריות שנתי, והשוואה לשנה קודמת אם קיימת. תמיד להריץ מחדש בסוף כל עדכון חודשי - זול לחשב.

9. **אופטימיזציית נקודות/הטבות**: אם `config/card_benefits.yaml` מולא (לא ריק), הרץ `python3 scripts/points_optimizer.py`. הסקריפט משווה את ההוצאה בפועל של החודש לפי קטגוריה וכרטיס מול ההטבות שהוגדרו, ומציע אם היה כדאי להשתמש בכרטיס אחר לקטגוריה מסוימת. אם הקובץ ריק - דלג בשקט, אל תדווח על זה.

10. **נקה**: העבר כל קובץ מ-inbox ששועבד בהצלחה לתת-התיקייה `processed/` שלו (כדי שלא יעובד שוב).

11. **Git commit**: `git add -A && git commit -q -m "עדכון <חודש>: N תנועות חדשות"` - שומר היסטוריה מלאה בלי מאמץ מהמשתמש.

12. **דווח למשתמש** בסוף, בקצרה ובלי הקדמות מיותרות: כמה תנועות חדשות נוספו, סה"כ הוצאות/הכנסות של החודש, נטו, חריגות אם יש, המלצת אופטימיזציה אם רלוונטית, ונתיב לקבצי הדשבורד (חודשי + שנתי).

## כללים כלליים
- מינימום שאלות למשתמש — רק כשבאמת חייבים (מיפוי עמודות חדש לגמרי / בית עסק גדול ולא מזוהה).
- שמור תמיד שינויים ב-`config/parsers.yaml` ו-`config/categories.yaml` כדי שהפעם הבאה תהיה מהירה ואוטומטית לגמרי.
- אל תמציא נתונים. אם קובץ פגום/לא ניתן לקריאה — דווח על כך במפורש ואל תנחש.
- הוסף שורת סיכום קצרה ל-`CHANGELOG.md` (תאריך ריצה, כמה תנועות נוספו) בסוף כל ריצה.
