# מערכת ניהול פיננסי — הוראות הפעלה

מערכת לאיחוד דפי אשראי ובנק ממספר מקורות לקובץ תנועות אחד, סיווג קטגוריות,
והפקת דוחות.

**קונבנציית הסימן הפנימית של המערכת:**

```
הוצאה  = סכום שלילי   (-250.00)
הכנסה  = סכום חיובי   (+8500.00)
```

כל פרסר חייב להמיר לקונבנציה הזו. זו נקודת הכשל מספר אחת — ראה "כיול סימן" למטה.

---

## הרצה שוטפת

```bash
python3 scripts/run_all.py
```

מריץ את הצינור המלא: קליטה → דדופ → סיווג → דוחות. אם כל המקורות מכוילים,
זה רץ מקצה לקצה בלי שאלה אחת.

הרצת דמו על נתוני דוגמה סינתטיים:

```bash
python3 scripts/run_all.py --demo
```

---

## הצינור

| שלב | סקריפט | קלט | פלט |
|---|---|---|---|
| זיהוי מבנה | `scripts/detect.py` | `inbox/<מקור>/*` | הצעת מיפוי ל-`config/parsers.yaml` |
| קליטה + דדופ | `scripts/ingest.py` | `inbox/**` + `parsers.yaml` | `data/transactions.csv` |
| סיווג | `scripts/categorize.py` | `data/transactions.csv` + `categories.yaml` | `data/transactions.csv` (מעודכן) |
| דוחות | `scripts/report.py` | `data/transactions.csv` | `reports/*.html`, `reports/*.md` |

---

## שלב חד-פעמי: כיול מקור חדש

כשמופיע קובץ ממקור שאין לו פרופיל ב-`config/parsers.yaml`:

```bash
python3 scripts/detect.py inbox/cal/some-file.xlsx
```

הסקריפט מדפיס את מבנה הקובץ, מנחש מיפוי עמודות, ומציג שורות קיצון לצורך
אימות. **הוא לא כותב ל-parsers.yaml לבד** — הכיול נכנס רק אחרי אישור.

### כיול סימן — חובה, בלי ניחוש

`detect.py` מציג את שורת הסכום הגדולה ביותר ואת הקטנה ביותר בקובץ. השאלה
היחידה שצריך לענות עליה: **בשורה שאתה בטוח לגביה — קנייה בסופר, משיכה,
תדלוק — האם הסכום בקובץ מופיע כחיובי או כשלילי?**

| מה שרואים בקובץ הגולמי | ערך `amount_sign` |
|---|---|
| הוצאה מופיעה כמספר חיובי (`250.00`) | `expense_positive` |
| הוצאה מופיעה כמספר שלילי (`-250.00`) | `expense_negative` |
| שתי עמודות נפרדות — חובה / זכות | `two_columns` |

רוב דפי האשראי בישראל הם `expense_positive` (הכל חיוב, אז אין סימן).
רוב דפי עובר-ושב הם `expense_negative` או `two_columns`. **אלה נטיות, לא
כללים — תמיד לאמת מול שורה ידועה.**

אחרי האימות מסמנים בקובץ:

```yaml
calibration:
  verified: true
  verified_on: "2026-08-14"
  sample_row: "שופרסל דיל | 12/07/2026 | 250.00"
  note: "אומת מול קנייה בסופר — מופיע חיובי בקובץ, הומר לשלילי"
```

`ingest.py` **מסרב לעבד** מקור עם `verified: false` ויוצא עם שגיאה. זו הגנה
מכוונת: עדיף להיעצר מאשר לייצר דוח עם סימן הפוך.

---

## זיהוי כרטיס לקובץ

כל כרטיס הוא חשבון נפרד עם `id` משלו. ההתאמה בין קובץ לחשבון נעשית לפי
`match` בפרופיל, בסדר הזה:

1. `filename_contains` — 4 ספרות אחרונות בשם הקובץ
2. `content_contains` — מחרוזת בתוך הקובץ עצמו (למשל מספר כרטיס בכותרת)
3. אם שניהם נכשלים — **לשאול את המשתמש פעם אחת** לאיזה כרטיס הקובץ שייך,
   ואז להוסיף `filename_contains` כדי שזה לא יישאל שוב.

---

## הטבות צבירה

`config/card_benefits.yaml` מחזיק את הטבות הצבירה לכל כרטיס. אם הקובץ ריק,
`report.py` מדלג על דוח האופטימיזציה ומדפיס הערה.

צריך לשאול את המשתמש **פעם אחת, בשאלה אחת מרוכזת** — לכל הכרטיסים יחד:
שיעור צבירה, סוג (נקודות / כסף / מיילים), קטגוריות מועדפות, ותקרה חודשית.

אחרי שהקובץ מלא, `report.py` מחשב לכל קטגוריה איזה כרטיס היה מחזיר הכי הרבה,
ומדווח כמה הפסדת בפועל על ידי חיוב בכרטיס הלא-אופטימלי.

---

## חריגות

`report.py` מסמן:

- קטגוריה שחרגה ביותר מ-40% מהממוצע החודשי שלה (מינימום 3 חודשי היסטוריה)
- תנועה בודדת מעל פי 3 מהחציון של הקטגוריה שלה
- חיוב כפול חשוד — אותו בית עסק, אותו סכום, בטווח 3 ימים, שלא נתפס בדדופ
- בית עסק חדש עם חיוב מעל 500 ₪

---

## דדופ

מפתח: `sha1(תאריך | סכום | בית-עסק-מנורמל | חשבון)`.

זה מטפל בחפיפה בין קבצים — למשל כשדף חודשי כולל תנועות שכבר הופיעו בדף הקודם.
הנרמול של שם בית העסק מוריד רווחים כפולים, מספרי סניף וסיומות מסוף.

---

## מקורות מייל

נבדק ב-14/08/2026:

- **CAL** — שולח מכתבים מ-`cal.info@icc.co.il`, אבל הקובץ נעול מאחורי לינק
  וסיסמת SMS. **לא ניתן לאוטומציה** — צריך להוריד ידנית ל-`inbox/cal/`.
- **HOT** — `service@hot-info.net.il`, "פירוט החשבון החודשי", מגיע קבוע וניתן
  לאוטומציה. חשבון תקשורת, לא כרטיס.
- **כביש 6** — `kvish6@printernet.co.il`, פירוט שנתי.
- לא נמצאו דוחות חודשיים פתוחים של max / ישראכרט / בנק.

---

## פרטיות

`.gitignore` חוסם `inbox/` (הקבצים הגולמיים), `data/` ו-`reports/`. שום נתון
פיננסי אמיתי לא נכנס ל-git. `samples/` מכיל נתונים סינתטיים בלבד ומותר לקומיט.
